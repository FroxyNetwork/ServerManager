Incoming:
> auth <id> <token>			<== Authentificate a Server via WebSocket
> start <uuid> <type>		<== Start a new server
> stop <id>					<== Stop a running server
> new <id>					<== Register a new server

Outcoming:
> auth <id> <token>			<== Authentificate this ServerManager via WebSocket
> auth						<== Say that specific server is authenticated
> register <uuid> <id>		<== Create a new server
> error <uuid>				<== When an error occured while creating specific server